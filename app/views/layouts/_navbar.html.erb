<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href=" <%= tasks_path %> ">ToDo 18 Sep</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <% if user_signed_in? %>
      <a class="nav-link" href="#">Completados <%=
        Usertask.where(user_id: current_user.id, completado: true).count
        %> / <%= Task.all.count %><span
        class="sr-only">(current)</span></a>
        <%= link_to 'Salir', destroy_user_session_path, method: :DELETE, class: 'nav-item nav-link' %>

      <% else %>
      <a class="nav-item nav-link active" href="<%= new_user_session_path %>"> Ingresar <span class="sr-only">(current)</span></a>
      <a class="nav-item nav-link" href=" <%= new_user_registration_path %> "> Registro </a>

      <% end %>
    </div>
  </div>
</nav>
